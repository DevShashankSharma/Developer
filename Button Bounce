<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
         *{
            margin:0;
            padding:0;
            font-family: 'Poppins',sans-serif;
         }
         body{
            background: #f3f2ff;
         }
         .btn{
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%,-50%);
            font-size: 16px;
            font-weight: 500;
            padding: 15px 40px;
            background: linear-gradient(135deg,#8793e3,#f631a3);
            color: #fff;
            border-radius: 8px;
            border: 0;
            outline: 0;
            display: flex;
            align-items: center;
            cursor: pointer;
            box-shadow: 0 5px 10px rgba(0,0,0.25);
            animation: bounce 0.5s cubic-bezier(0.8,0,1,1) infinite alternate;
        }
         @keyframes bounce{
            100%{
                margin-top:30px;
            }
         }
    </style>
</head>
<body>
    <button class="btn">Button</button>
</body>
</html>
